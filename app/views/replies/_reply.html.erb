<div class="post-replies my-2">
    <%= link_to reply.user.fullname, user_path(reply.user) %><br>
    @<%= link_to reply.user.username, user_path(reply.user) %><br>

    <p><%= reply.content %></p>

    <div class="quote-icon">
     <%= delete_reply(reply) %>
    </div>
</div>
